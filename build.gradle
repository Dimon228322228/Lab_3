plugins {
    id 'war'
    id 'java'
}

group 'Dimon228322228'
version '1.0'

repositories {
    mavenCentral()
    mavenCentral()
}

ext {
    junitVersion = '5.8.2'
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

dependencies {

    testImplementation 'junit:junit:4.13.1'
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.8.1'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.8.1'
    testImplementation 'junit:junit:4.13.1'
    testImplementation 'junit:junit:4.13.1'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.8.1'

    providedCompile 'org.projectlombok:lombok:1.18.24'
    annotationProcessor 'org.projectlombok:lombok:1.18.24'

    implementation 'jakarta.servlet:jakarta.servlet-api:6.0.0'
    implementation 'jakarta.faces:jakarta.faces-api:4.0.1'
    implementation 'jakarta.annotation:jakarta.annotation-api:2.1.1'
    implementation 'jakarta.transaction:jakarta.transaction-api:2.0.1'
    implementation 'jakarta.enterprise:jakarta.enterprise.cdi-api:4.0.1'
    implementation 'jakarta.persistence:jakarta.persistence-api:3.1.0'

    implementation 'org.primefaces:primefaces:12.0.0:jakarta'

    implementation 'org.postgresql:postgresql:42.5.1'

    providedRuntime('org.eclipse.persistence:eclipselink:4.0.0-RC2')
    providedRuntime('org.wildfly:jipijapa-eclipselink:27.0.1.Final')

    // providedRuntime - not packaged in war (provided by container)

    implementation("org.junit.jupiter:junit-jupiter-api:5.8.1")
    implementation("org.junit.jupiter:junit-jupiter-engine:5.8.1")
    implementation("org.junit.jupiter:junit-jupiter-params:5.8.1")
    implementation("org.junit.platform:junit-platform-suite:1.8.1")

    implementation 'com.google.code.gson:gson:2.10'
}

test {
    useJUnitPlatform()
}

tasks.register('deploy') {
    dependsOn 'build'
    doLast {
        exec {
            workingDir '.'
            commandLine 'sh', '-c', 'scp build/libs/webLab-1.0.war helios:~/wildfly-27.0.0.Final/standalone/deployments'
        }
    }
}